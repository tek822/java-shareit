--DROP TABLE users IF EXISTS CASCADE;
--DROP TABLE items IF EXISTS CASCADE;
--DROP TABLE bookings IF EXISTS CASCADE;
--DROP TABLE item_requests IF EXISTS CASCADE;
--DROP TABLE comments IF NOT EXISTS CASCADE;

CREATE TABLE IF NOT EXISTS users (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY (START WITH 1 INCREMENT BY 1) PRIMARY KEY,
    name VARCHAR(64) NOT NULL,
    email VARCHAR(100) NOT NULL
);

CREATE TABLE IF NOT EXISTS items (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY (START WITH 1 INCREMENT BY 1) PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    description VARCHAR(255) NOT NULL,
    available BOOLEAN,
    user_id BIGINT,
    request_id BIGINT
);

CREATE TABLE IF NOT EXISTS bookings (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY (START WITH 1 INCREMENT BY 1) PRIMARY KEY,
    start_time TIMESTAMP,
    end_time TIMESTAMP,
    item_id BIGINT,
    user_id BIGINT,
    status VARCHAR(50)
);

CREATE TABLE IF NOT EXISTS item_requests (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY (START WITH 1 INCREMENT BY 1) PRIMARY KEY,
    description VARCHAR(255),
    user_id BIGINT,
    created TIMESTAMP
);


CREATE TABLE IF NOT EXISTS comments (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY (START WITH 1 INCREMENT BY 1) PRIMARY KEY,
    text VARCHAR,
    item_id BIGINT,
    user_id BIGINT,
    created TIMESTAMP
);

ALTER TABLE users ADD CONSTRAINT IF NOT EXISTS email_unique UNIQUE (email);

ALTER TABLE comments ADD CONSTRAINT IF NOT EXISTS comments_users_user_id_fk
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE;

ALTER TABLE comments ADD CONSTRAINT IF NOT EXISTS comments_items_item_id_fk
    FOREIGN KEY (item_id) REFERENCES items(id) ON DELETE CASCADE;

ALTER TABLE items ADD CONSTRAINT IF NOT EXISTS items_users_user_id_fk
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE;

ALTER TABLE items ADD CONSTRAINT IF NOt EXISTS items_item_requests_requeset_id_fk
    FOREIGN KEY (request_id) REFERENCES item_requests(id) ON DELETE CASCADE;

ALTER TABLE bookings ADD CONSTRAINT IF NOT EXISTS bookings_users_user_id_fk
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE;

ALTER TABLE bookings ADD CONSTRAINT IF NOT EXISTS bookings_items_item_id_fk
    FOREIGN KEY (item_id) REFERENCES items(id) ON DELETE CASCADE;

ALTER TABLE item_requests ADD CONSTRAINT IF NOT EXISTS item_requests_users_user_id_fk
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE;
